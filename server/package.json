{
  "name": "wedding-invitation-server",
  "private": true,
  "version": "1.0.0",
  "description": "Self-hosted static site + RSVP API for the wedding invitation, backed by a JSON file (no database service to run or debug remotely).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
